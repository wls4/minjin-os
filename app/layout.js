import './globals.css';

export const metadata = { title: 'MINJIN OS', description: 'macOS-style project workspace' };

export default function RootLayout({ children }) {
  return <html lang="ko"><body>{children}</body></html>;
}
